import { resolve } from './functions/resolve'
import { promise } from './functions/promise'
import { then } from './functions/then'
import { parallel } from './functions/parallel'

export const Promise_ = {
  promise,
  resolve,
  then,
  parallel
}
